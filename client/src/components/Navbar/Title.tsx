import styled from "styled-components";

export const Title = styled.h1`
  display: inline;
  margin: 0;
  margin-right: 1.5rem;
  padding: 0;

  color: var(--color-primary);
  font-size: 2rem;
  font-weight: 900;
`;
